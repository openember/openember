menu "Core Runtime"

comment "Framework library (openember::core). Enabled by OpenEmber Link."

config OPENEMBER_ENABLE_CORE
    bool
    default y if OPENEMBER_ENABLE_LINK
    depends on OPENEMBER_ENABLE_LINK

endmenu
