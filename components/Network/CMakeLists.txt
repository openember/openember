project(Network VERSION 0.1.0 LANGUAGES CXX)

set(LIB_NETWORK_SRCS
    socket_uri.cpp
)

OPENEMBER_ADD_STATIC_LIBRARY(TARGET ${PROJECT_NAME}
    SRCS     ${LIB_NETWORK_SRCS}
    LIBS     ${OPENEMBER_EXTERNAL_LIBRARIES}
    INCLUDES ${OPENEMBER_EXTERNAL_INCLUDE_DIRS} ${OPENEMBER_INTERNAL_INCLUDE_DIRS} ${CMAKE_CURRENT_SOURCE_DIR}
    DESTINATION lib
)

set_property(TARGET Network PROPERTY CXX_STANDARD 17)
set_property(TARGET Network PROPERTY CXX_STANDARD_REQUIRED ON)

