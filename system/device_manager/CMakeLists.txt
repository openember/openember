project(device_manager VERSION 1.0.0 LANGUAGES CXX)

set( DEVICE_MODULE_SRCS 
    main.cpp
)

OPENEMBER_ADD_EXECUTABLE(TARGET ${PROJECT_NAME} 
    SRCS     ${DEVICE_MODULE_SRCS} 
    LIBS     ${OPENEMBER_LIBRARIES} openember_core
    INCLUDES ${OPENEMBER_INCLUDE_DIRS}
             ${CMAKE_SOURCE_DIR}/core/include
    DESTINATION bin 
)
