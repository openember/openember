project(Template VERSION 1.0.0)

set( TEMPLATE_MODULE_SRCS 
    main.c
)

AGLOO_ADD_EXECUTABLE(TARGET ${PROJECT_NAME} 
    SRCS     ${TEMPLATE_MODULE_SRCS} 
    LIBS     ${AGLOO_LIBRARIES}
    INCLUDES ${AGLOO_INCLUDE_DIRS}
    DESTINATION bin 
)
